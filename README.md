# Document Analyzer

CLI app: paste a URL, fetch page text, analyze with OpenAI.

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# Edit .env and set your OpenAI API key
```

## Run

```bash
python app.py
```
